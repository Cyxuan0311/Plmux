# plmux — lightweight tmux-like PTY multiplexer (Rich + C extension)
rich>=13,<15
ptyprocess>=0.7,<0.8; sys_platform != "win32"
blessed>=1.20,<2
jinxed>=1.1.0,<2; sys_platform == "win32"

# Windows: optional but recommended for reliable PTY with older hosts
pywinpty>=3,<4; sys_platform == "win32"

# Development / Testing
pytest>=7,<9
pytest-asyncio>=0.21,<1
